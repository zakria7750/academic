# توسيع نظام الشهادات لدعم الملفات والصور

## ملخص التحديثات

تم توسيع نظام الشهادات ليدعم كل من الصور والملفات (PDF/DOCX) بالإضافة إلى الصور الموجودة حالياً.

## الملفات المحدثة

### 1. قاعدة البيانات
- **ملف جديد**: `scripts/extend-certificates-table.sql`
  - إضافة أعمدة جديدة: `certificate_file`, `content_type`, `file_name`, `file_size`
  - تحديث العمود `certificate_image` ليكون اختياري
  - إضافة قيود للتأكد من وجود محتوى (صورة أو ملف)

### 2. أنواع البيانات
- **محدث**: `lib/supabase.ts`
  - تحديث نوع `Certificate` لدعم الحقول الجديدة
  - إضافة نوع `content_type` مع القيم `'image' | 'file'`

### 3. إجراءات الخادم
- **محدث**: `app/actions/certificates-actions.ts`
  - تحديث `addCertificate()` لدعم الملفات والصور
  - تحديث `updateCertificate()` لدعم الملفات والصور
  - إضافة التحقق من أنواع الملفات المسموحة
  - إضافة دالة `downloadCertificateFile()` لتحميل الملفات

### 4. صفحة التحقق
- **محدث**: `app/verification/page.tsx`
  - تحديث واجهة `Certificate`
  - إضافة دعم عرض الملفات مع أيقونات مناسبة
  - إضافة أزرار "عرض الملف" و "تحميل الملف"
  - إضافة دالة `formatFileSize()` لعرض حجم الملف
  - الحفاظ على عرض الصور كما هو بدون تغيير

### 5. لوحة التحكم (جزئي)
- **محدث جزئياً**: `app/dashboard/certificates/page.tsx`
  - تحديث واجهة `Certificate`
  - تحديث نموذج البيانات لدعم الملفات
  - تحديث دوال المعالجة

## الميزات الجديدة

### 1. دعم أنواع الملفات
- **الصور**: JPEG, PNG, WebP (كما هو موجود سابقاً)
- **الملفات**: PDF, DOCX (جديد)

### 2. عرض الشهادات
- **الصور**: عرض كما هو سابقاً مع إمكانية العرض بالحجم الكامل والتحميل
- **الملفات**: عرض معلومات الملف (اسم، نوع، حجم) مع أزرار:
  - **عرض الملف**: فتح الملف في نافذة جديدة
  - **تحميل الملف**: تحميل الملف إلى جهاز المستخدم

### 3. التحقق من الملفات
- التحقق من نوع الملف قبل الرفع
- التحقق من وجود محتوى (صورة أو ملف)
- عرض رسائل خطأ واضحة للمستخدم

## خطوات التطبيق المطلوبة

### 1. تحديث قاعدة البيانات
```sql
-- تشغيل الملف التالي في قاعدة بيانات Supabase
-- scripts/extend-certificates-table.sql
```

### 2. تحديث لوحة التحكم (مطلوب إكمال)
يحتاج ملف `app/dashboard/certificates/page.tsx` إلى تحديث نموذج إضافة/تعديل الشهادات لإضافة:
- خيار اختيار نوع المحتوى (صورة أو ملف)
- حقل رفع الملفات
- تحديث واجهة المستخدم لعرض نوع المحتوى

### 3. اختبار النظام
- اختبار إضافة شهادة بصورة
- اختبار إضافة شهادة بملف PDF
- اختبار إضافة شهادة بملف DOCX
- اختبار التحقق من الشهادات
- اختبار تحميل وعرض الملفات

## الملاحظات التقنية

### 1. تخزين الملفات
- الصور: `certificates/images/`
- الملفات: `certificates/files/`
- استخدام Vercel Blob للتخزين

### 2. الأمان
- التحقق من أنواع الملفات المسموحة
- التحقق من أحجام الملفات
- استخدام روابط آمنة للتحميل

### 3. تجربة المستخدم
- عرض مختلف للصور والملفات
- أزرار واضحة للعرض والتحميل
- رسائل خطأ واضحة
- دعم اللغة العربية

## التوافق مع النظام الحالي

- **الشهادات الموجودة**: ستعمل بدون مشاكل (تُعتبر من نوع 'image')
- **واجهة التحقق**: تدعم كلا النوعين تلقائياً
- **قاعدة البيانات**: التحديث متوافق مع البيانات الموجودة

## المتطلبات التقنية

- Next.js 14+
- Supabase
- Vercel Blob
- TypeScript
- Tailwind CSS
- Lucide Icons

---

**ملاحظة**: تم إكمال معظم التحديثات. يبقى فقط إكمال تحديث لوحة التحكم وتشغيل اختبارات شاملة للنظام.