# إعداد رفع الشهادات - دليل حل المشاكل

## المشاكل الشائعة وحلولها

### 1. مشكلة "خدمة رفع الملفات غير مُعدة بشكل صحيح"

**السبب:** عدم وجود متغير البيئة `BLOB_READ_WRITE_TOKEN`

**الحل:**
1. انتقل إلى لوحة تحكم Vercel
2. اذهب إلى Storage → Blob
3. أنشئ Blob Store جديد إذا لم يكن موجوداً
4. احصل على `BLOB_READ_WRITE_TOKEN`
5. أضف المتغير في ملف `.env.local`:
```
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxx
```

### 2. مشكلة "نوع الملف غير مدعوم"

**السبب:** محاولة رفع ملف بصيغة غير مدعومة

**الحل:** تأكد من أن الملف بإحدى الصيغ التالية:
- JPEG (.jpg, .jpeg)
- PNG (.png)
- WebP (.webp)

### 3. مشكلة "حجم الملف كبير جداً"

**السبب:** حجم الملف أكبر من 5 ميجابايت

**الحل:** 
- ضغط الصورة قبل الرفع
- استخدام أدوات ضغط الصور مثل TinyPNG
- تغيير دقة الصورة إذا لزم الأمر

### 4. مشكلة "فشل في رفع صورة الشهادة"

**الأسباب المحتملة:**
- مشكلة في الاتصال بالإنترنت
- مشكلة في خدمة Vercel Blob
- خطأ في إعدادات المتغيرات البيئية

**الحلول:**
1. تحقق من الاتصال بالإنترنت
2. تأكد من صحة `BLOB_READ_WRITE_TOKEN`
3. تحقق من حالة خدمة Vercel في status.vercel.com

### 5. مشكلة "رقم الشهادة موجود مسبقاً"

**السبب:** محاولة إضافة شهادة برقم موجود بالفعل

**الحل:** استخدم رقم شهادة مختلف أو قم بتحديث الشهادة الموجودة

## المتغيرات البيئية المطلوبة

```env
# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxx

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

## التحسينات المضافة

### 1. التحقق من صحة الملفات
- فحص نوع الملف قبل الرفع
- فحص حجم الملف (حد أقصى 5MB)
- رسائل خطأ واضحة ومفصلة

### 2. معالجة أفضل للأخطاء
- رسائل خطأ مخصصة لكل نوع من المشاكل
- تسجيل مفصل للأخطاء في الكونسول
- التحقق من المتغيرات البيئية

### 3. تحسينات واجهة المستخدم
- عرض الأنواع المدعومة والحد الأقصى للحجم
- التحقق الفوري من الملف عند الاختيار
- رسائل تأكيد واضحة

## اختبار النظام

### 1. اختبار رفع ملف صحيح
```
1. اختر صورة JPEG أو PNG أقل من 5MB
2. أدخل رقم شهادة فريد
3. اختر تاريخ الإصدار
4. اضغط "إضافة الشهادة"
5. يجب أن تظهر رسالة نجاح
```

### 2. اختبار رفع ملف خاطئ
```
1. اختر ملف PDF أو Word
2. يجب أن تظهر رسالة خطأ فوراً
3. جرب ملف أكبر من 5MB
4. يجب أن تظهر رسالة خطأ مناسبة
```

## الدعم الفني

إذا استمرت المشاكل:
1. تحقق من الكونسول في أدوات المطور
2. راجع سجلات Vercel
3. تأكد من صحة إعدادات Supabase
4. تحقق من حالة خدمات Vercel