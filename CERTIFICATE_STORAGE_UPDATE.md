# تحديث نظام تخزين الشهادات لدعم الملفات المختلفة

## نظرة عامة

تم تطوير النظام لدعم تخزين وعرض الملفات المختلفة (الصور والمستندات) في قاعدة البيانات مباشرة بدلاً من استخدام خدمات التخزين الخارجية.

## التغييرات المطلوبة

### 1. تحديث قاعدة البيانات

يجب تشغيل الـ SQL script التالي لتحديث هيكل الجدول:

```sql
-- تشغيل الملف: scripts/alter-certificates-table.sql
-- أو تشغيل الأوامر التالية مباشرة:

-- إضافة عمود مؤقت جديد
ALTER TABLE certificates ADD COLUMN certificate_image_new BYTEA;

-- حذف العمود القديم
ALTER TABLE certificates DROP COLUMN certificate_image;

-- إعادة تسمية العمود الجديد
ALTER TABLE certificates RENAME COLUMN certificate_image_new TO certificate_image;
```

### 2. الملفات المحدثة

#### أ. ملفات جديدة:
- `lib/file-utils.ts` - وظائف مساعدة لاكتشاف أنواع الملفات ومعالجتها
- `scripts/alter-certificates-table.sql` - تحديث هيكل قاعدة البيانات

#### ب. ملفات محدثة:
- `app/actions/certificates-actions.ts` - تعديل server actions لدعم تخزين الملفات
- `app/verification/page.tsx` - تحديث واجهة التحقق لعرض الملفات المختلفة

## الميزات الجديدة

### 1. دعم أنواع الملفات المختلفة

النظام يدعم الآن:
- **الصور**: JPEG, PNG, GIF, WebP, BMP, TIFF
- **المستندات**: PDF, DOCX, DOC, RTF, TXT
- **ملفات أخرى**: أي نوع ملف آخر

### 2. اكتشاف نوع الملف التلقائي

- يتم اكتشاف نوع الملف بناءً على البايتات (Magic Bytes)
- لا يعتمد على امتداد الملف فقط
- دقة عالية في التعرف على أنواع الملفات

### 3. تخزين البيانات الوصفية

يتم تخزين المعلومات التالية مع كل ملف:
- البيانات المشفرة (Base64)
- نوع MIME
- الاسم الأصلي للملف
- حجم الملف

### 4. واجهة مستخدم محسنة

#### للصور:
- عرض مباشر في صفحة التحقق
- إمكانية عرض بالحجم الكامل
- زر تحميل الصورة

#### للملفات الأخرى:
- عرض معلومات الملف (النوع، الحجم، الاسم)
- زر تحميل الملف
- أيقونة مناسبة لنوع الملف

## التوافق مع النظام القديم

النظام يدعم التوافق مع البيانات القديمة:
- الشهادات المخزنة كروابط URL ستعمل بشكل طبيعي
- لا حاجة لتحديث البيانات الموجودة
- التحديث تدريجي عند إضافة شهادات جديدة

## كيفية الاستخدام

### 1. رفع الملفات

```typescript
// في النموذج، يمكن رفع أي نوع ملف
const formData = new FormData()
formData.append('certificateImage', file) // file يمكن أن يكون صورة أو PDF أو أي ملف آخر
```

### 2. عرض الملفات

النظام يحدد تلقائياً كيفية عرض الملف:
- **الصور**: تظهر مباشرة في الصفحة
- **الملفات الأخرى**: تظهر كبطاقة معلومات مع زر تحميل

### 3. تحميل الملفات

```typescript
// تحميل الملف برمجياً
const result = await downloadCertificateFile(certificateId)
if (result.success) {
  // تحويل البيانات إلى ملف قابل للتحميل
  const bytes = base64ToBytes(result.fileData.data)
  const blob = new Blob([bytes], { type: result.fileData.mimeType })
  // إنشاء رابط تحميل
}
```

## الأمان والأداء

### 1. التحقق من نوع الملف
- فحص البايتات للتأكد من نوع الملف الحقيقي
- منع رفع ملفات ضارة مقنعة

### 2. ضغط البيانات
- تخزين البيانات كـ Base64 في JSON
- إمكانية إضافة ضغط في المستقبل

### 3. حدود الحجم
- يمكن إضافة حدود لحجم الملفات المرفوعة
- مراقبة استخدام مساحة قاعدة البيانات

## استكشاف الأخطاء

### 1. مشاكل عرض الصور
- تأكد من صحة تشفير Base64
- تحقق من نوع MIME المحدد

### 2. مشاكل تحميل الملفات
- تأكد من وجود البيانات في قاعدة البيانات
- تحقق من صلاحيات المتصفح للتحميل

### 3. مشاكل قاعدة البيانات
- تأكد من تشغيل script تحديث الجدول
- تحقق من نوع العمود (BYTEA)

## التطوير المستقبلي

### 1. ميزات مقترحة
- ضغط الملفات قبل التخزين
- معاينة المستندات (PDF viewer)
- دعم ملفات متعددة لكل شهادة
- تحسين الأداء للملفات الكبيرة

### 2. تحسينات الأمان
- فحص محتوى الملفات للتأكد من الأمان
- تشفير البيانات الحساسة
- تسجيل عمليات الوصول للملفات

## الخلاصة

تم تطوير النظام بنجاح لدعم:
✅ تخزين الملفات المختلفة في قاعدة البيانات
✅ اكتشاف نوع الملف تلقائياً
✅ عرض الصور مباشرة
✅ تحميل الملفات الأخرى
✅ التوافق مع النظام القديم
✅ واجهة مستخدم محسنة