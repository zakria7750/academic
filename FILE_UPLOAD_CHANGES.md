# تحديث نظام رفع الملفات - تخزين الملفات في قاعدة البيانات

## ملخص التغييرات

تم تحديث المشروع لحفظ الملفات مباشرة في قاعدة البيانات كـ `bytea` بدلاً من استخدام خدمات التخزين الخارجية مثل Vercel Blob.

## الملفات المحدثة

### 1. `app/actions/certificates-actions.ts`
- **إزالة**: استيراد `@vercel/blob`
- **إضافة**: دوال لتحويل الملفات إلى `Uint8Array`
- **إضافة**: دالة `convertFileDataToDataURL()` لتحويل البيانات إلى Data URLs للعرض
- **إضافة**: دالة `isImageFile()` للتحقق من نوع الملف
- **إضافة**: دالة `downloadCertificateFile()` لتحميل الملفات
- **تحديث**: جميع دوال CRUD لتعمل مع البيانات الجديدة

### 2. `app/verification/page.tsx`
- **تحديث**: واجهة `Certificate` لتدعم الأعمدة الجديدة
- **إضافة**: دالة `handleDownloadFile()` لتحميل الملفات
- **تحديث**: عرض الملفات لدعم الصور والملفات الأخرى
- **إضافة**: عرض مختلف للصور مقابل الملفات الأخرى

### 3. `app/dashboard/certificates/page.tsx`
- **تحديث**: واجهة `Certificate` لتدعم الأعمدة الجديدة
- **تحديث**: نماذج رفع الملفات لدعم أنواع ملفات متعددة
- **إضافة**: عرض نوع الملف واسم الملف في قائمة الشهادات
- **تحديث**: قبول أنواع ملفات متعددة: `image/*,.pdf,.doc,.docx,.txt`

## تغييرات قاعدة البيانات

### أعمدة جديدة في جدول `certificates`:
- `file_type VARCHAR(255)`: نوع MIME للملف
- `file_name VARCHAR(255)`: اسم الملف الأصلي

### العمود الموجود:
- `certificate_image`: تم تغييره من URL إلى `BYTEA` لحفظ بيانات الملف

## أنواع الملفات المدعومة

- **الصور**: JPG, PNG, GIF, WebP, SVG
- **المستندات**: PDF, DOC, DOCX
- **النصوص**: TXT

## كيفية العمل

### رفع الملفات:
1. المستخدم يختار ملف من النموذج
2. يتم تحويل الملف إلى `ArrayBuffer`
3. يتم تحويل `ArrayBuffer` إلى `Uint8Array`
4. يتم حفظ البيانات في عمود `certificate_image` مع معلومات الملف

### عرض الملفات:
1. يتم جلب البيانات من قاعدة البيانات
2. يتم تحويل `Uint8Array` إلى Base64
3. يتم إنشاء Data URL للعرض في المتصفح
4. للصور: عرض مباشر في `<img>`
5. للملفات الأخرى: عرض أيقونة مع زر تحميل

### تحميل الملفات:
1. يتم إنشاء Data URL من البيانات المحفوظة
2. يتم إنشاء رابط تحميل مؤقت
3. يتم تشغيل التحميل تلقائياً

## المزايا

1. **لا حاجة لخدمات خارجية**: جميع الملفات محفوظة في قاعدة البيانات
2. **أمان أكبر**: الملفات محمية بنفس مستوى حماية قاعدة البيانات
3. **نسخ احتياطية موحدة**: الملفات تُنسخ مع باقي البيانات
4. **دعم أنواع ملفات متعددة**: ليس فقط الصور

## ملاحظات مهمة

1. **حجم الملفات**: تأكد من أن حجم الملفات مناسب لقاعدة البيانات
2. **الأداء**: للملفات الكبيرة، قد تحتاج لتحسينات إضافية
3. **النسخ الاحتياطية**: ستكون أكبر حجماً بسبب الملفات المحفوظة

## تشغيل Migration

قم بتشغيل الملف `database-migration.sql` على قاعدة البيانات لإضافة الأعمدة الجديدة.

```sql
-- في Supabase SQL Editor أو أي أداة إدارة قواعد البيانات
-- قم بتشغيل محتويات ملف database-migration.sql
```

## اختبار التحديثات

1. تأكد من أن قاعدة البيانات محدثة
2. جرب رفع ملفات مختلفة (صور، PDF، إلخ)
3. تأكد من عرض الملفات بشكل صحيح
4. جرب تحميل الملفات
5. تأكد من عمل البحث والتحقق من الشهادات